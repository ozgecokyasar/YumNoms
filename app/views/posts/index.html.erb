<% if user_signed_in? %>
<div class="placeslist-container">
  <div class="places-list">
    <% @posts.each do |post| %>
    <%= link_to post.title, post_path(post) %>
    <hr>
    <% end %>
  </div>
</div>
<% end %>
